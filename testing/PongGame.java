package testing;

public class PongGame {
    // Minimal test-only constants/getters so the menu can read sizes
    public static final int WINDOW_WIDTH = 1640;
    public static final int WINDOW_HEIGHT = 1480;

    public static int getWindowWidth() { return WINDOW_WIDTH; }
    public static int getWindowHeight() { return WINDOW_HEIGHT; }
}